<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>StudyBuddy - Collaborative Study Platform</title>
    <link rel="stylesheet" href="/css/style.css">
    
    
</head>
<body>
    <!-- Animated background particles -->
    <div class="bg-particles">
        <div class="particle"></div>
        <div class="particle"></div>
        <div class="particle"></div>
        <div class="particle"></div>
        <div class="particle"></div>
        <div class="particle"></div>
    </div>

    <!-- Navigation -->
    <nav>
        <div class="nav-container">
            <a href="/" class="logo">StudyBuddy</a>
            <ul class="nav-links">
                <li><a href="#features">Features</a></li>
                <li><a href="#about">About</a></li>
                <li><a href="#contact">Contact</a></li>
            </ul>
        </div>
    </nav>

    <!-- Main hero section -->
    <main>
        <div class="hero">
            <h1>Study Smarter, Together</h1>
            <p>Join the ultimate collaborative study platform where students and developers connect, share knowledge, and achieve their goals in real-time study rooms.</p>
            
            <div class="cta-container">
                <form action="/new" method="POST" style="display: contents;">
                    <button type="submit" class="btn btn-primary">
                        🚀 Create Room
                    </button>
                </form>
                <button class="btn btn-secondary" onclick="openJoinModal()">
                    👥 Join Room
                </button>
            </div>
        </div>
    </main>

    <!-- Features section -->
    <section class="features" id="features">
        <div class="features-container">
            <h2>Why Choose StudyBuddy?</h2>
            <div class="features-grid">
                <div class="feature-card">
                    <h3>🎯 Real-time Collaboration</h3>
                    <p>Work together in synchronized study sessions with live chat, shared whiteboards, and instant feedback.</p>
                </div>
                <div class="feature-card">
                    <h3>📚 Smart Study Tools</h3>
                    <p>Access flashcards, note sharing, code editors, and productivity timers all in one seamless platform.</p>
                </div>
                <div class="feature-card">
                    <h3>🌟 Global Community</h3>
                    <p>Connect with students and developers worldwide. Find study partners who share your interests and goals.</p>
                </div>
            </div>
        </div>
    </section>

    <!-- Join Room Modal -->
    <div id="joinModal" class="modal">
        <div class="modal-content">
            <h2>Join Study Room</h2>
            <form id="joinForm" method="GET">
                <input type="text" id="roomCode" name="roomCode" placeholder="Enter room code" required>
                <div class="modal-buttons">
                    <button type="button" class="btn-cancel" onclick="closeJoinModal()">Cancel</button>
                    <button type="submit" class="btn-join">Join Room</button>
                </div>
            </form>
        </div>
    </div>

    <script>
        function openJoinModal() {
            document.getElementById('joinModal').style.display = 'block';
            document.getElementById('roomCode').focus();
        }

        function closeJoinModal() {
            document.getElementById('joinModal').style.display = 'none';
            document.getElementById('roomCode').value = '';
        }

        // Close modal when clicking outside
        window.onclick = function(event) {
            const modal = document.getElementById('joinModal');
            if (event.target === modal) {
                closeJoinModal();
            }
        }

        // Handle join form submission
        document.getElementById('joinForm').addEventListener('submit', function(e) {
            e.preventDefault();
            const roomCode = document.getElementById('roomCode').value.trim();
            if (roomCode) {
                // Redirect to room with the provided code
                window.location.href = `/${roomCode}`;
            }
        });

        // Add smooth scrolling for navigation links
        document.querySelectorAll('a[href^="#"]').forEach(anchor => {
            anchor.addEventListener('click', function (e) {
                e.preventDefault();
                const target = document.querySelector(this.getAttribute('href'));
                if (target) {
                    target.scrollIntoView({
                        behavior: 'smooth',
                        block: 'start'
                    });
                }
            });
        });

        // Add entrance animations on scroll
        const observerOptions = {
            threshold: 0.1,
            rootMargin: '0px 0px -50px 0px'
        };

        const observer = new IntersectionObserver((entries) => {
            entries.forEach(entry => {
                if (entry.isIntersecting) {
                    entry.target.style.animation = 'fadeInUp 0.6s ease-out forwards';
                }
            });
        }, observerOptions);

        document.querySelectorAll('.feature-card').forEach(card => {
            observer.observe(card);
        });
    </script>
</body>
</html>